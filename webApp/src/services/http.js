import http from 'http';

function () {

}

export default Http{
  static httpModth(options){
    
  }
  static myMethod(msg) {
    console.log('static', msg);
  }

  myMethod(msg) {
    console.log('instance', msg);
  }
}
